{
  "name": "majormapper",
  "description": "Compare core classes accross multiple UC curriculums",
  "version": "0.0.1",
  "main": "scrapper.mjs",
  "dependencies": {
    "express": "^4.18.3"
  },
  "scripts": {
    "client": "cd src/client && tsc && cd ../.. && node src/utils/build-cleanup.mjs --script",
    "server": "cd src/server && tsc && cd ../..",
    "analytics": "cd src/analytics && tsc && cd ../..",
    "build": "npm run client && npm run server && npm run analytics",
    "start": "cd out/server && node server.js"
  },
  "keywords": [],
  "author": "Colin Davis",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.16.5"
  }
}

import { Curriculum, Vertex, Edge, Metrics } from '../types/analytics'

/** Metrics are based on https://curricularanalytics.org/help/metrics */
export async function CalculateMetrics(curriculum: Curriculum): Promise<void> {
    return
}